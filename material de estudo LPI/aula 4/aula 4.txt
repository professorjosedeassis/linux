Comandos:

ls -l -> lista arquivos, diretórios e mais informações
ls --color -> lista arquivos, diretórios com cores

tipo 
-    rw- rw- r--

-  arquivo
d  diretório
l  link

rm -> remove um arquivo
rm -r -> remove um diretório de forma recursiva
rm -rf -> remove qualquer coisa de forma forçada (sem dó nem piedade)
rm -rf /* -> "JUSTA CAUSA"

> -> direciona a saída, sobrescrevendo o arquivo
>> -> direciona a saída, adicionando o conteúdo do arquivo

Dica: Executando 2 comandos simultaneamente>

comando1 ; comando2
________________________________________________________________
Personalizando o Servidor Linux

issue -> "Tela de logon"
apt-get install linuxlogo

issue.linuxlogo
no diretório etc
fazer um bakup do arquivo issue
sobrescrever o arquivo usando  >
cat issue.linuxlogo > issue

Mensagem de boas vindas
vi /etc/motd

escrever a nova mensagem

_____________________________________________________
Personalizando o GRUB
GRUB -> gerenciador de boot
Atenção !!! Cuidado ao mexer no GRUB

lilo -> obsoleto
grub legacy -> obsoleto, porém cai na LPI
grub2 -> atual

caminho:
/etc/default/grub

modificar as linhas abaixo:

# aumentar o tempo do grub
GRUB_TIMEOUT=20

# mudar a resolução da tela do grub
# descomentar e modificar a linha abaixo
GRUB_GFXMODE=800x600

# mudar a resolução do terminal(bash)
GRUB_GFXPAYLOAD_LINUX=800x600

# instalando e configurando um papel de parede no GRUB
# "propaganda", personalização, etc
# Passo1: instalar o pacote grub2-splashimages
# apt-get install grub2-splashimages
# Passo 2: cleobter a imagem da mesma resolução do GRUB
# a imagem obrigatóriamente tem que ser no formato .tga
# acrescentar a linha abaixo no grub
GRUB_BACKGROUND="/usr/share/images/grub/Plasma-lamp.tga"

update-grub

____________________________________________________________

Montando um pen drive no Linux
Passo 1: Verificar se o dispositivo foi identificado pelo Linux
lsusb
fdisk -l

Passo2: Anotar o tipo de partição identificada pelo Linux
Ex:
/dev/sdb1
/dev/sdc1

Passo3: Montar o dispositivo no diretório mnt ou outro de sua preferência

mount -t vfat /dev/sdb1 /mnt

mount -> montagem de dispositivos
-t vfat -> sistema de arquivos
/dev/sdb1 -> dispositivo
/mnt -> destino


*** Para desmontar o dispositivo:
- Sair do diretório mnt
- Digitar o comando
umount /dev/sdb1

____________________________________________

Gerenciando serviços:

apt-get install rcconf
rcconf